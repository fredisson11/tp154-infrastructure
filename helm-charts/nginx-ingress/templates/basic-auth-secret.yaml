apiVersion: v1
kind: Secret
metadata:
  name: {{ .Chart.Name }}-basic-auth-secrets
type: Opaque
data:
  auth: {{ printf "%s:%s" .Values.basicAuthUsername (bcrypt .Values.basicAuthPassword) | b64enc }}