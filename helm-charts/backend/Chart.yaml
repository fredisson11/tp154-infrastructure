apiVersion: v2
name: backend
description: Helm chart for deploying the backend application (Python, REST API, PostgreSQL)
type: application

# Це версія Helm-чарту (інфраструктурна, не додатку)
version: 1.0.0

# Це версія самого додатку (збігається з Docker-образом)
appVersion: "1.0.0"

# Автор (опціонально, але бажано)
maintainers:
  - name: fredisson11

# Джерело (опціонально)
sources:
  - https://github.com/your-org/backend
